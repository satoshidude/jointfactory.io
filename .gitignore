# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-dev
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment & data
.env
data/
server/index.js.backup.*

# Database copies, wherever they land. Snapshots pulled for local testing and
# the pre-deploy backups on the server hold every player's npub, balance and
# game state — one `git add -A` in the wrong directory would publish the lot.
backups/
*.db
*.db-wal
*.db-shm
*.sqlite*

# Agent/editor scratch that only makes sense on one machine
.claude/
AGENTS.md
CLAUDE.md
tasks/

# Project notes and local maintenance/analysis tools are not part of the
# installable application.
konzepte/
scripts/
ANALYSE_UND_KONZEPT.md
LIGHTNING_MINES_KONZEPT.md
PITCH_GARDEN_AND_EMPIRES.md
